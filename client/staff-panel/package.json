{
  "name": "staff-panel",
  "version": "0.0.0",
  "dependencies": {
    "@lib/shared": "*"
  },
  "devDependencies": {},
  "scripts": {
    "type-check": "tsc --noEmit",
    "build-only": "vite build",
    "dev": "vite",
    "build": "run-p type-check \"build-only {@}\" --",
    "preview": "vite preview"
  }
}
