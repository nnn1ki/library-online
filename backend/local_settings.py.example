from app.settings import *

OPAC_HOSTNAME = "https://library.istu.edu/opac"
SERVICE_HOSTNAME = "http://localhost:5173"

OAUTH_CLIENT_ID = ""
OAUTH_CLIENT_SECRET = ""
OPAC_INTERNAL_TOKEN=""

EMAIL_HOST = "smtp.mail.ru"
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = ""
EMAIL_HOST_PASSWORD = ""
DEFAULT_FROM_EMAIL = EMAIL_HOST_USER
SERVER_EMAIL = DEFAULT_FROM_EMAIL

# EMAIL_MODE = "prod"
EMAIL_MODE = "test"
# EMAIL_MODE = "off"

NOTIFICATION_DIGEST_WINDOW_MINUTES = 60
NOTIFICATION_ACTIVE_HOURS = 2
NOTIFICATION_TIME_ZONE = "Asia/Irkutsk"

# for SQLite
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.sqlite3",
        "NAME": BASE_DIR / "db.sqlite3",
    }
}

# for PostgreSQL
# DATABASES = {
#     "default": {
#         "ENGINE": "django.db.backends.postgresql",
#         "NAME": "dbname",
#         "USER": "postgres_superuser",
#         "PASSWORD": "password",
#         "HOST": "localhost",
#         "PORT": "",
#     }
# }
